#include <stdio.h>
#include <string.h>
typedef struct telephone
{
    char name[20];
    char address[30];
    int number;
} x;
int main()
{
    int y=65;
    for(int i=0;i<10;i++){
        char ch=(int)y;
        x.(ch);        
        fgets(a.name,sizeof(ch.name),stdin);
        fgets(a.address,sizeof(ch.address),stdin);
        scanf("%d",&ch.number);
        y++;
    }
    printf("%d",B.address);
    return 0;
}
