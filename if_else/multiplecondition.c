#include <stdio.h>
int main()
{
    int n;
    printf("Enter a number : ");
    scanf("%d", &n);
    if (n > 5)
    {
        printf("The number is greter than 5");
    }
    else
    {
        printf("the number is not greter than 5");
    }
    return 0;
}